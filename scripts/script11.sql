
CREATE TABLE student_data2 (
           student_id int primary key,
           student_name varchar(25),
             student_age int,
             gender char (1),
             DOA date,
             city_name varchar(20)
 );
